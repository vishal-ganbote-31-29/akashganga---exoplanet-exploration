Place the provided Spiderman image file into this project's `assets` folder with the exact filename `spiderman_full.png`.

Steps:
1. Download the Spiderman image you attached in the chat.
2. Save it as `spiderman_full.png`.
3. Put the file in the `assets/` folder at the project root: `assets/spiderman_full.png`.

Behavior after placing the file:
- The app will automatically load `/assets/spiderman_full.png` for the main Spidey full-body illustration (used in `LoginPage`, `NewsPage`, etc.).
- If the browser fails to load the image, the app will fall back to an inline SVG vector illustration.

If you'd like, I can try to write the image into the repo for you — upload the PNG directly in the workspace or tell me to proceed and I'll attempt to create it from the attachment.